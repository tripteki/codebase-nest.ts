"use strict";

import col from "underscore";

/**
 * @function
 *
 * @param {any} cols
 * @returns {any}
 */
export const collectionHelper = (cols: any): any => col (cols);

export default collectionHelper;
